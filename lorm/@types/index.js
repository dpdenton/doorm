"use strict";
exports.__esModule = true;
var ActionType;
(function (ActionType) {
    ActionType["SELECT"] = "SELECT";
    ActionType["FROM"] = "FROM";
    ActionType["JOIN"] = "JOIN";
    ActionType["JOIN_LATERAL"] = "JOIN_LATERAL";
    ActionType["GROUP"] = "GROUP";
    ActionType["WHERE"] = "WHERE";
    ActionType["SQL"] = "SQL";
})(ActionType = exports.ActionType || (exports.ActionType = {}));
